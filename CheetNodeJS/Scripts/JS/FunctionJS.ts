//Function 終わり
function Function() {

    //コンテキストと引数リスト(配列)を指定して関数を実行する
    func.apply();

    //コンテキストを指定して新しい関数を作る
    func.bind();

    //関数が期待する引数の数を取得する
    func.length;

    //コンテキストと引数が指定して関数を実行する
    func.call();

}
