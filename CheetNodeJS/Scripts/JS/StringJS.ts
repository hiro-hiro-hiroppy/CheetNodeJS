class StringJS{

    constructor() {

    }

    public String





}



//String 終わり
function String() {

    //文字列を末尾から延長する
    str.padEnd('延長する文字数', '延長に使う文字列');

    //文字列から一文字取得する
    str.charAt('インデックス番号');

    //特定の文字列はどの位置にあるか後方から調べる
    str.lastIndexOf('', '検索開始位置');

    //文字列を繰り返す
    str.repeat('回数');

    //文字列の一部を取り出す
    str.slice(数字);

    //特定の文字列を含むか調べる
    str.includes('文字列', No);

    //文字列を大文字に変換する
    str.toUpperCase();

    //文字列を小文字に変換する
    str.toLowerCase();

    //特定の文字列で終わるか調べる
    str.endsWith('');

    //文字列を先頭から延長する
    str.padStart(2, 0);

    //文字列を取得する
    str.length();

    //特定の文字列はどの位置にあるか前方から調べる
    str.indexOf('', '検索開始位置');

    //文字列内の両端の空白を削除する
    str.trim();
    str.trimStart();
    str.trimEnd();

    //正規表現系

    //文字列を置換する(ここはもう少し詰める)
    str.replace('');

    //正規表現で文字列のマッチングをする(ここはもう少し詰める)
    str.match('');

    //文字列内に特定のパターンがあるか調べる
    str.search('');
}
