//Array 終わり
function Array() {

    //配列の末尾の要素を取り出す
    arr.pop();

    //配列の各要素に実行
    arr.forEach(function () { })

    //配列の要素少なくとも一つは条件を満たしているか調べる
    arr.some(function () { });

    //配列の各要素に実行した関数のやつで配列を作る
    arr.map(function () { });

    //配列の各要素に関数を実行し、一つの値にまとめる
    arr.reduce(function () { });

    //配列からイテレーターを作る(これもう少し突き詰める)
    arr.values(function () { });

    //配列の先頭に要素を追加する
    arr.unshift('');

    //配列の一部を取り出す(-の場合も調べる)
    arr.slice(2, 4);

    //配列を結合する
    arr.concat(arr2);

    //配列の要素の順番を反転させる
    arr.reverse();

    //配列の要素全てが条件を満たしているかを調べる
    arr.every(function () { });

    //配列の先頭の要素を取り出す
    arr.shift();

    //配列内の条件を満たす最初の要素を取り出す
    arr.find(function () { });

    //配列の特定の要素のインデックスを調べる
    arr.indexOf('');

    //配列内で要素をコピーする(もうちょい調べる)
    arr.copyWithin(0, 1);

    //配列内の要素を結合して文字列にする
    arr.join('区切り文字');

    //配列の末尾に要素を追加する
    arr.push('');

    //配列に特定の要素が含まれるか調べる
    arr.includes();

    //配列内の条件を満たす要素からなる配列を作る
    arr.filter(function () { });

    //配列の要素数を取得・設定する
    arr.length;

    //配列の要素をソートする(もうちょい調べる)
    arr.sort(function () { });

    //配列から要素を削除・追加する
    arr.splice();

    //配列内を任意の値で満たす
    arr.fill();



}